language: php

sudo: false

php:
    - 7.3

cache:
    directories:
        - $HOME/.composer/cache

before_script:
    - composer install

script:
    - composer phpstan
    - composer php-cs-fixer
    - vendor/bin/phpunit --exclude-group=tmux
    - vendor/bin/deptrac
