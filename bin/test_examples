#!/usr/bin/env bash
set -xe

for fname in ./example/*.json; do
    ./bin/maestro run --plan="$fname"
done

for fname in ./example/**/*.json; do
    ./bin/maestro run --plan="$fname"
done

echo "Done"
exit 0
